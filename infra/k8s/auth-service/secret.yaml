apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: risk-platform
type: Opaque
stringData:
  # Do not commit real secrets.
  POSTGRES_PASSWORD: "replace-me-local-only"
  BANKING_CORE_JWT_SECRET: "replace-me-local-only-32-char-secret"
