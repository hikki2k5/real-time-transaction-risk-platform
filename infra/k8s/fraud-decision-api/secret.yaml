apiVersion: v1
kind: Secret
metadata:
  name: fraud-decision-api-secret
  namespace: risk-platform
type: Opaque
stringData:
  # Placeholder only. Override locally before using real credentials.
  # Do not commit real secrets.
  POSTGRES_PASSWORD: "replace-me-local-only"
