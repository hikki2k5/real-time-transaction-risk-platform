apiVersion: v1
kind: Service
metadata:
  name: banking-core
  namespace: risk-platform
  labels:
    app: banking-core
spec:
  type: NodePort
  selector:
    app: banking-core
  ports:
    - name: http
      port: 8084
      targetPort: http
      nodePort: 30084
