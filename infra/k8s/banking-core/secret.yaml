apiVersion: v1
kind: Secret
metadata:
  name: banking-core-secret
  namespace: risk-platform
type: Opaque
stringData:
  # Placeholder for future sensitive banking-core config.
  # Do not commit real secrets.
  PLACEHOLDER_SECRET: "replace-me-local-only"
